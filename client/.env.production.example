# Plantilla para build de producción (Vercel).
# En Vercel no uses este archivo: configura las variables en el dashboard.
# Si pruebas build local: cp .env.production.example .env.production

VITE_BACKEND_URL=https://REPLACE_WITH_YOUR_RENDER_BACKEND_URL
