# =============================================================================
# MiAyudaTIC — Frontend — build de producción local
# Vercel: NO uses este archivo; configura variables en el dashboard.
# Uso local: cp .env.production.example .env.production
# =============================================================================

VITE_BACKEND_URL=https://REPLACE_WITH_YOUR_RENDER_BACKEND_URL
